import React, { Component } from "react";
import styles from "../components/LiveContainer.module.css";

export default class LiveContainer extends Component {
  render() {
    return (
      <>
        <div className={styles.container}>hola mundo</div>
      </>
    );
  }
}
